# secrets — ห้ามขึ้น GitHub เด็ดขาด
.env
*.key
*.pem
secrets.*
config.local.*

# python
__pycache__/
*.pyc
.venv/
venv/

# data cache — data/ ตอนนี้ track ไว้จริง (อัปเดตวันละครั้งผ่าน GitHub Action, ใช้แทนการยิง yfinance สด)
*.csv
